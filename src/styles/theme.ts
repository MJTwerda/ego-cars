import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#171717',
      light: '#9d9b9b',
    },
    secondary: {
      main: '#EB0A1E',
    },
  },
});

export default theme; 
